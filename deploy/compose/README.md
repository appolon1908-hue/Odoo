# Isolated staging runtime placement

Do not invent a Compose topology in this repository before the active Odoo host layout, volume ownership, database service, worker count, reverse-proxy path, and rollback target are audited.

The staging operator must consume the exact protected merged artifact by digest, mount only reviewed custom add-ons, restore a sanitized database into an isolated database name, keep every live capability false, and record the effective Compose or orchestration manifest as evidence. This source-candidate branch changes no server.
