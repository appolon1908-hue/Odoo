# Candidate integration overlay only. Apply after source/digest/restore certification.
services:
  odoo:
    environment:
      CODESTRA_CALLING_REALTIME_ENABLED: ${CODESTRA_CALLING_REALTIME_ENABLED:-false}
      CODESTRA_CALLING_MIDDLEWARE_DIGEST: ${CODESTRA_CALLING_MIDDLEWARE_DIGEST:-}
      CODESTRA_CALLING_GATEWAY_DIGEST: ${CODESTRA_CALLING_GATEWAY_DIGEST:-}
      CODESTRA_CALLING_SDK_DIGEST: ${CODESTRA_CALLING_SDK_DIGEST:-}
