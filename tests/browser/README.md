# Browser certification

The Playwright specification is intentionally dormant unless both isolated-staging URLs are supplied. It rejects canonical production and infrastructure hosts before any browser action. The fixture must contain synthetic data and an authorized test agent.

Required evidence includes screen-pop accuracy, refresh recovery, campaign and tenant denial, mandatory wrap-up, keyboard operation, reduced-motion behavior, supported desktop sizes, and p95 timing.
