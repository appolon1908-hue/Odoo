# Rollback

Remove dashboard menus or restore the disposable pre-upgrade database. KPI and
export evidence is immutable and should be retained for audit. This module has
no external delivery worker and performs no production report distribution.
