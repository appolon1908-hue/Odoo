# Changelog

## 18.0.2.0.0

- Depend on `codestra_base` and bridge existing role groups without moving XML IDs or memberships.
- Preserve the installed core and compatibility sync models while adding the single callback model.
- Add callback-time, call-metric, duplicate-call, duration, relationship, role-bridge, XML-ID, phone-normalization, and fail-closed regression coverage.
- Exclude never-installed automation, transfer-request, compliance, AI, QA, and notification definitions from active core loading for later owner addons.
- Keep all external/live capabilities disabled.

## 18.0.1.0.0

- Initial clean rebuild with staged integration models and signed event intake.
