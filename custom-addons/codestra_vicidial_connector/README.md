# Codestra VICIdial Connector

Offline, fail-closed VICIdial normalization and import-preview addon. The only adapter type is `TEST_SYN`; profiles default inactive and test-only. It has no HTTP, database, Redis, socket, shell, cron, controller, credential value, live import, call-control, or external-delivery implementation.
