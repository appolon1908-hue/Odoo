# Security

Only Integration Administrators receive connector administration access. Profiles store external configuration references, never credential values. Live activation and application of preview records fail closed.
