# Rollback

Disable downstream reporting first, uninstall only on a disposable database, or
restore the pre-upgrade snapshot. Adherence and exception evidence is append-only
and is not deleted by an operational rollback. No external workforce or dialer
state is mutated by this module.
