# Codestra Contact Center Compliance

Mission facade for consent evidence, DNC and suppression, channel opt-outs, calling windows, destination timezone, recording disclosure, approved caller IDs, jurisdiction policy, retention, and complaints.

Every customer communication must pass the campaign, consent, sender, destination, geographic, template, and provider gates. A missing or ambiguous policy fails closed.
