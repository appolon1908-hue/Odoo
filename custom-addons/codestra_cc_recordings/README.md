# Codestra contact-center recordings

This module binds existing VICIdial/Asterisk recording metadata to one canonical
campaign, call, agent membership, customer profile, telephony mapping, and
approved recording policy. It stores no media binary or unrestricted object URL.

Playback and AI assistance are globally disabled. The module adds no public
controller or network client. Access and retention evidence is append-only.
