# Rollback

Disable the module in a disposable environment and restore the pre-upgrade
database. Canonical bindings and evidence must be retained or exported under the
approved retention process; do not delete authoritative legacy metadata or
storage objects from Odoo. No external storage mutation is performed by this
module.
